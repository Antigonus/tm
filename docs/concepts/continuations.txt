

 Continuations are listed as optional so that they may have defaults.  We set the default
 for these optional arguments to directly return values.  In this manner the default
 behavior for a function with continuations the same as that of an old style function
 that does not have continuations.



